import type{Page}from"@giraffle/domain";import{Pressable,StyleSheet,Text,View}from"react-native";import{Icon,type IconName}from"@/components/ui/primitives";import{useTheme}from"@/design/ThemeProvider";import{radii,spacing,typography}from"@/design/tokens";import{useApp}from"@/state/AppProvider";
export function PageMetaBar({page,onOpenPlanning}:{page:Page;onOpenPlanning():void}){const{colors}=useTheme();const{snapshot}=useApp();const state=snapshot.states.find((item)=>item.id===page.stateId);const items:{key:string;label:string;icon:IconName;accent?:boolean}[]=[{key:"state",label:state?.title??"State",icon:state?.family==="done"?"checkmark-circle-outline":state?.family==="open"?"ellipse-outline":"bookmark-outline",accent:state?.family==="open"},{key:"priority",label:page.priority?page.priority==="do"?"Focus":page.priority==="schedule"?"Plan":page.priority==="delegate"?"Delegate":"Drop":"Priority",icon:"flag-outline"},{key:"schedule",label:page.scheduledAt?page.scheduledAt.replace("T"," · "):"Schedule",icon:"calendar-outline"},{key:"duration",label:page.durationMinutes?`${page.durationMinutes} min`:"Duration",icon:"timer-outline"}];return <View style={styles.bar}>{items.map((item)=><Pressable key={item.key} accessibilityRole="button" onPress={onOpenPlanning} style={({pressed})=>[styles.chip,{borderColor:colors.border,backgroundColor:item.accent?colors.accentSubtle:pressed?colors.hover:"transparent"}]}><Icon name={item.icon} size={14} color={item.accent?colors.accent:colors.muted}/><Text style={[typography.caption,{color:item.accent?colors.accent:colors.secondary}]}>{item.label}</Text></Pressable>)}</View>}
const styles=StyleSheet.create({bar:{flexDirection:"row",flexWrap:"wrap",gap:spacing.xs,marginBottom:spacing.lg},chip:{minHeight:32,paddingHorizontal:spacing.sm,borderWidth:StyleSheet.hairlineWidth,borderRadius:radii.full,flexDirection:"row",alignItems:"center",gap:6}});
