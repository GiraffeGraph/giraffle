# Bootstrap-only env vars. Everything else (OpenAI, OAuth providers, upload
# dir, log level, update repo) is stored encrypted in the database and managed
# at /settings/secrets after first login.

DATABASE_URL="postgresql://giraffle:giraffle_dev_2024@localhost:5432/giraffle?schema=public"
NEXTAUTH_URL="http://localhost:3000"
AUTH_SECRET="replace-with-a-long-random-secret"

# Optional: separate encryption key for the secret box. Defaults to AUTH_SECRET.
# APP_ENCRYPTION_KEY=""
