interface:
  display_name: "Graffle Backlog Executor"
  short_description: "Execute the next Graffle backlog slice"
  default_prompt: "Use $graffle-backlog-executor to pick the next ready Graffle task, execute it safely, and update the backlog."
